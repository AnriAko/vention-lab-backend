import { Injectable } from '@nestjs/common';

//TODO - check connection with service + add health checker for dev env and maybe for prod env too

@Injectable()
export class RedisService {
    // Redis connection setup belongs here, if needed later.
}
